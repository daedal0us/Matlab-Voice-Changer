# ---------------------------------------------------------------- 音频数据
# 排除所有 wav（包含各子目录，Git 的 * 会跨目录匹配）
*.wav

# 其他常见音频格式（需要保留其中某种时，在这一行下面写 !名字.wav 来取消排除）
*.mp3
*.flac
*.m4a
*.aac
*.ogg
*.opus

# ---------------------------------------------------- MATLAB 本地产物
# 自动保存 / 临时文件
*.asv
*.m~
*.autosave
*.slxc
*.mltbx

# 性能分析 / 覆盖率报告
*.prof
slprj/
codegen/
html/
coverage/

# ---------------------------------------------------------- 编辑器 / 系统
.vscode/
.idea/
*.code-workspace
Thumbs.db
desktop.ini
.DS_Store
