# 开发日志

记录**每次改动做了什么、为什么、以及哪些路走过并且否掉了**。

`README.md` 只描述当前状态（一版一版重写，不保留旧版本对比）；**"和以前相比变了什么"写在这里**。

已完成的改动按会话归并，最近的在最前。回溯更早的逐次实验原始记录见
[`NOTES_dev_draft.md`](NOTES_dev_draft.md)（早期草稿，含当时被推翻的结论）。

---

## 2026-09-23 · 相位锁定与文档重构

### 相位锁定成为默认（`5007ffe`、`94a60c5`）

**做了什么**：`vc_pitchshift_pv` 实装 Laroche & Dolson（ICMC-1997 式 8）的 identity phase
locking，并作为默认开启；`--no-phase-lock` 保留旧的每 bin 独立递推。

**为什么**：这是"金属感"的主答案。相位声码器丢失帧内各分音的相位关系（垂直相干性），
听感即涂抹/金属。A/B 试听（电平已对齐）确认开启后金属感大幅减轻。

**判断依据是听感，这一点必须记住。** 五个物理指标没有一个能复现听感结论：
帧速率伪影谱线在真实语音上只是换位置；HNR 换窗口后两版基本相同；谱质心/高频占比几乎不动；
帧速率包络调制反而升高；每分音相位游走只在平稳信号上灵敏（受控元音 ↓2.3×）。
**光看数会得出相反的结论**，所以默认值的依据写在代码注释与 README 里。

**调通过程中修掉的实现错误**（每个都产出"能正常播、只是更差"的音频）：

1. **共享了峰的"相位增量"而不是"相位旋转"**。正确式子是
   `angle_Y(m,k) = angle_Y(m,p) + angle_X(m,k) − angle_X(m,p)`，共享的是旋转量
   `θ_p = angle_Y(m,p) − angle_X(m,p)`，每个 bin 保留自己的分析相位。共享增量会让区内
   相位关系每帧旋转 `|k−p|·2π·hop_out/N`，把分音拧散（谐波能量 −6 %、宽带噪声翻倍）。
2. **用合成相位驱动峰的递推**。`θ_p` 需要**分析**相位，而 `P` 是逐帧被覆盖的，必须另存副本
   （`Xang`），否则旋转量成为循环定义。
3. **峰判据自相矛盾**：对称的"大于左右邻居"配上端点复制式 padding 等价于 `seg > seg`，
   永远不成立 → 0 个峰/帧，**整段锁定静默失效**（输出与未锁定逐位相同）。
4. **按"记录最大值"分区**：该规则单调，一个区会跨越多个分音并把它们压成一个频率。
5. **按 bin 做漂移钳位**：同区两个 bin 被截断成不同量，破坏锁定要保护的关系（改为按区）。
6. 曾把分音位移 39 Hz 归因于**频率分辨率**——实测峰频率估计精度 <1 Hz
   （100 % 的估计落在真实分音 5 Hz 内），真正原因是分区跨过了未检测到的分音。

**试过并否掉的更便宜的修法**（不要再试）：把 `hop_out` 强制到整数网格
（−66.7 vs −67.7 dB，无变化）；每帧每 bin 加 ±0.5π 随机相位（−28.6 vs −28.7 dB，无变化）。

**自检**：`demo_voice_changer` 新增断言——锁定必须真的降低相位游走（受控元音 ≥1.3×），
且开/关输出必须不同。它能捕获"锁定退化成静默空操作"。

### HNR 结论更正（`00213d4`）

曾记录"锁定让 HNR 从 +4.47 掉到 −0.73 dB"，并据此理解为"用伪影换噪声"。
在真实文件上**扫 HNR 窗口宽度**后推翻：±10 Hz 处锁定反而好 0.68 dB，±25 Hz 处只差 0.12 dB，
±60 Hz（80 % 能量被正确归类）处差 0.23 dB。差距在任何窗口下都 ≤0.7 dB。
**那个"变差"是固定窄窗容不下分音轻微移动造成的假象。**
附带发现该方法的失效边界：窗口 >±72 Hz 时谐波带互相重叠，谐波占比算出 121 %/159 %。
复现脚本 `hnrwidth.m`。

### 基频读数的信任问题（`8de65f1`、`ee31264`、`d0b0442`）

自由搜索的跟踪器在变声后的音频上错多于对：同一次 child 转换随搜索带读出
177.7 / 190.9 / 287.4 / 347.7 Hz，而设计输出约 210 Hz；其中 190.9 Hz 落在旧的 ±25 %
"合理性带"内，于是**把错数字当成正常测量报了出来**。

改为**以"本次转换应有的基频"为中心、±6 % 宽**的窄带重新分析，报告写明
`narrow-band estimate, +-6 %`；窄带内找不到周期时给出设计值并注明未经独立验证。
实测合成 120 Hz 元音转 child：349.6 Hz（偏 66 %）→ 209.5 Hz。窄带 ±12/±6/±3 % 三种宽度
结果只差几个百分点，而自由搜索会差一倍。

**试过并否掉的"判断估计是否可信"思路**（记在 `vc_analyze.m`）：
**带宽稳定性**（干净合成元音反而给出 4.49 的相对散布，而错得离谱的转换只给 0.006）；
**谐波梳/谐波积谱支持度**（所有候选彼此相差 0.6 dB 以内；积谱更差，把干净的 120 Hz 元音
判成 110 Hz）。

### 电平规则改为匹配输入 RMS（`abf5b85`）

原规则是"归一化到固定 −18 dBFS"，再有削顶保护二次压低：139 s 测试文件最终 RMS 0.0707，
**比输入低 6.6 dB**。改为**输出 RMS 跟随输入**（A/B 试听不该被响度差干扰）。

实现时踩到一点：转换后的信号会带**个别尖峰**（峰值达 RMS 目标刻度的 2.45 倍，波峰因数
24.5 dB）。整段按该尖峰缩放会把输出砍掉 19 dB（RMS → 0.0075），等于让一个样本决定整段响度。
改为 **0.9 以上软削顶**（`tanh` 过渡），实测只影响 0.046 % 的样本（拐点取 0.5 则波及 1.9 %）。
削顶上限从"−18 dBFS 电平"改为 0.999 的**限幅保护**；`--target-level` 仍接受但不再决定电平。

`demo_voice_changer` 新增断言：输出 RMS 与输入之差 ≤0.5 dB。

### GUI（`84da2be` 起，含后续三次修复）

从零加图形界面：打开 → 选预设 → 转换 → A/B 试听。设计取舍与踩过的坑：

* **只留预设、不给自由参数滑杆**——预设里每个数字是成套调好的，单独暴露一个会让用户拼出
  从未验证过的组合（典型是共振峰倍率超过基频倍率）。
* **后台转换用定时器轮询 `Future.State`，不用 `afterEach`**：`afterEach` 把回调投递到事件
  队列，"worker 跑完"与"回调被派发"是两件事，实测出现过输出文件已写好而界面永远停在
  "转换中"、控件全灰只能关窗。轮询后加了 15 分钟看门狗与关窗取消。
* **两个控件放同一个网格单元格不会报错，只会叠在一起**——一次改动让"打开"按钮跨到第 1 列，
  正好压住预设下拉框，导出的截图里下拉框整个消失而 `Layout` 仍报告它在原位。
* **`plot` 少传一个参数不报错，只画错**：重构时漏掉时间轴，x 轴变成桶序号，波形渲染成一排短横。
* **`uiaxes` 文字默认 TeX 解释**，`child_female` 的下划线被当成下标。
* **`uiputfile` 的第三个输出是"选中的筛选器索引"**，曾是"切到 MP3 再切回 WAV 没反应"的原因：
  它不补扩展名，返回的名字带着默认名的扩展名，于是选格式不改变任何文本。改为由筛选器索引
  决定扩展名。
* 波形改用 **min/max 包络抽取**（6.1e6 样本 → 12000 顶点），否则 139 s 录音是一次秒级重绘。
* `app_check.m` 为上述问题提供断言：代码分析、控件行列与**单元格冲突**、坐标轴里**真实画出的
  数据**（XData/YData 跨度）、解释器设置、扩展名规则、一次真实转换后**界面是否恢复**。

### 预设语义与上限修正（`4e8b296` → `d3096e3`，`79b1ba1`）

* **自动 F0 参考 + 比例上限**。上限一度写成"目标 / 参考"，于是**所有低于参考的输入都被反向
  钳制**，两个童声预设的输出顺序颠倒（child 207.9 Hz > child_female 195.5 Hz）。改为
  **目标 / 100 Hz 地板**，三个预设共用同一地板。
* **输出上限 `max_f0` 在 `--ref auto` 下失效**：输出已等于目标，上限只会把预设拉离自己的目标。
* **方向守卫**：童声预设不得把基频往下压（240 Hz 输入曾被压到 183.5 Hz，得到比原声更低的
  "童声"）。`--allow-down` 解除，报告标注 `[direction limited]`。
* **`child_female` 的设计参考 190 Hz** 但上限曾用 1.316，导致它比 210 Hz 的 child 抬得更少；
  上限改为 250/100 后恢复正常。
* **预设名描述目标音色而非输入性别**：`child` = 男孩感童声，`child_female` = 女孩感童声，
  任何输入都能配任何预设。名字保留不改（改名会让所有用到它的命令行失效）。

### 其他修复

* **行向量输入被压成 1 个样本**（`6276d56`）：`size(x,2) > 1` 对 1×N 行向量也成立，于是
  `mean(x,2)` 沿**时间轴**求平均，整段音频变成一个数，随后死在长度检查里报"input too short"。
  `audioread` 只返回列向量，所以只有内存传数组的调用者会踩到。改为 `~isvector(x)`。
* **`vc_resample` 抗混叠**（`5ebbbf5`）：截止频率原为 `min(1, ratio)`，而 `ratio > 1` 恰是它
  **抽取**的方向，等于没有抗混叠，输出奈奎斯特以上的内容折回 3–5 kHz。改为 `1/ratio` 并让
  滤波器长度随抽取倍数增长；6 kHz 纯音经 ×1.567 后残留从 −0.2 dB 降到 −21.0 dB。
* **`vc_args` 的 flag 吞掉文件名**（`672a234`）：判据曾是"下一个 token 不以 `--` 开头"，
  于是 `--no-formant-track out.wav` 把输出文件名吃掉了，转换照跑但不落盘，看起来像间歇性故障。
  改为按默认值的**类型**判断是否带值。
* **删除未使用的 `lvl` 局部变量**（`3b7f4c9`）：电平规则改为固定削顶上限后它不再被读取。

### 文档重构（`0b4cfc0`、本次）

原 README 长到 676 行、十节，且**自相矛盾**：§7 记录相位锁定已大幅改善金属感，§8 仍写着
同一问题"只能换 TD-PSOLA 根治"（写于实装之前）。改为：

* `README.md` **只描述当前状态**，一版一版重写，不做版本对比；
* 本文（`DEVLOG.md`）承接"改了什么、为什么、否掉了什么"；
* 原 README 原文保留为 `NOTES_dev_draft.md`。

---

## 更早的会话

以下是 `NOTES_dev_draft.md` 里已详细记录的阶段，此处只列主题：

* 变调方向配反（拉伸与重采样互换）导致音频加速 2.4 倍、尾部静音 —— 引出**独立的时间轴检查**。
* `vc_istft` 的 COLA 归一化在非整数 `hop_out` 下被近零除数放大成尖峰。
* 共振峰级修复：包络 lifter 长度、warp 映射方向、掩模插值、倾斜的双重补偿。
* 相位声码器"金属感"的第一轮测量：浊音帧谐波占比 −0.24 → −1.44 dB、波形残差 −0.7 dB，
  结论是**相位游走而非丢谐波**。
* PSOLA 尝试与回退：难点在基音标记检测（1500 个周期只出 583 个标记），标记错会导致**咬字消失**。
* 固定共振峰因子成为默认（放弃按音高缩放，理由是**可复现性**而非精度）。
